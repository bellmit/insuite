YUI.add( 'DataViewMojit', function( Y, NAME ) {
    

    /*
     * module wide variables and constants
     * @module DataViewMojit
     */

    Y.namespace( 'mojito.controllers' )[NAME] = {};

}, '3.16.0', { requires: [] } );
